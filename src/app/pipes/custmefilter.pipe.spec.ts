import { CustmefilterPipe } from './custmefilter.pipe';

describe('CustmefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustmefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
